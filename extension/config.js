// Local development defaults — replace API_URL / FRONTEND_URL for production (Render / Vercel)
const CONTINUUM_CONFIG = {
  API_URL: 'http://localhost:8000',
  FRONTEND_URL: 'http://localhost:3000',
  HANDOFF_CHAR_WARN: 32000,
};
